// 防抖useThrottleFn(() => {},1000)
// 节流useDebounceFn(() => {},1000)
// 转base64  useBase64()
// 克隆 const { cloned } = useCloned(original)
// 进度条  const { isLoading } = useNProgress()
// 二维码： npm i qrcode => const qrcode = useQRCode('') => <img :src="qrcode" alt="QR Code" />
// 小数点： const stringResult = useToFixed(value, 5, {type: 'string'})
// 缓存： const state = useStorage('my-store', { hello: 'hi', greeting: 'Hello' })
